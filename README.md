# Divide & Conquer

Simple web project containing an example implementation of divide & conquer algorithms.

Files:
- `index.html` — demo page
- `script.js` — JavaScript logic
- `style.css` — styles

How to use

1. Open `index.html` in a browser.
2. Or run a static server (e.g., `npx serve`) for testing.
